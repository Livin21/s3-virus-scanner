# Required: ARN of the S3 bucket to scan for malware
SOURCE_BUCKET_ARN=arn:aws:s3:::your-bucket-name

# Optional: ARN of the S3 bucket to quarantine infected files
# If not provided, infected files will be deleted without quarantine
QUARANTINE_BUCKET_ARN=arn:aws:s3:::your-quarantine-bucket-name

# Optional: Webhook URL to receive scan results (POST JSON)
# WEBHOOK_URL=https://your-webhook-endpoint.com/scan-results

# Optional: Environment name (default: dev)
# ENVIRONMENT=dev

# Optional: AWS Account ID (defaults to CDK_DEFAULT_ACCOUNT)
# ACCOUNT_ID=123456789012

# Optional: AWS Region (defaults to CDK_DEFAULT_REGION)
# REGION=us-east-1

